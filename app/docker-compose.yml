version: '3.8'

services:
  rasa:
    build: .
    ports:
      - "5005:5005"
    command: >
      rasa run --enable-api --cors "*"

  action_server:
    build: ./actions
    ports:
      - "5055:5055"
    command: >
      rasa run actions
